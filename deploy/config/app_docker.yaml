# environment
env: dev

log:
  level: "warn"
  environment: dev
  format: "console"
  app_id: ptcg_trader

database:
  debug: false
  host: postgres
  port: 5432
  username: local
  password: local
  db_name: ptcg
  type: postgres
  search_path: "trader"
  ssl_enable: false

redis:
  addresses: [ "redis:6379" ]
  password: ""
  max_retries: 5
  pool_size: 5
  db: 1

http:
  debug: false
  address: ":17420"
